package com.morpho.morphosmart.sdk;
/**
 * 
 * Disable or enable the latency detection for Capture operation.
 *
 */
public class LatentDetection {
    /** Disable the latency detection (this is the default). */
    public static final int LATENT_DETECT_DISABLE = 0;
    /** Enable the latency detection. */
    public static final int LATENT_DETECT_ENABLE = 1;

    /**
     * Private constructor, do not use.
     */
    private LatentDetection() {
    }
}
